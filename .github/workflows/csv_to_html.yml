name: Convert CSV to HTML and Excel

on:
  push:
    branches:
      - main
      - gitActions
    paths:
      - 'doc/*.csv' 

permissions:
  contents: write 

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v2

    - name: Set up Python
      uses: actions/setup-python@v2
      with:
        python-version: '3.x'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install pandas openpyxl

    - name: List CSV files
      run: |
        echo "Listing CSV files in the /doc/ directory"
        ls -l doc/*.csv || echo "No CSV files found in /doc/"

    - name: Convert CSV to HTML
      run: |
        python - <<EOF
        import pandas as pd
        import os

        # Create the output directory if it doesn't exist
        output_dir = "doc/html_output"
        os.makedirs(output_dir, exist_ok=True)

        # Define the files and their IDs
        files_with_ids = {
            "BackgroundDB_SourceDatasets_ecoinvent": "doc/BackgroundDB_SourceDatasets_ecoinvent.csv",
            "BackgroundDB_SourceDatasets_GaBi": "doc/BackgroundDB_SourceDatasets_GaBi.csv",
            "Common_references": "doc/Common_references.csv",
            "Country-specific_indicators": "doc/Country-specific_indicators.csv",
            "EN15804+A1_indicators": "doc/EN15804+A1_indicators.csv",
            "EN15804+A2_indicators": "doc/EN15804+A2_indicators.csv",
            "Flow_properties_and_unit_groups": "doc/Flow_properties_and_unit_groups.csv"
        }

        for file_id, file_path in files_with_ids.items():
            if os.path.exists(file_path):
                df = pd.read_csv(file_path)
                html_table = df.to_html(index=False, classes='table table-striped', border=0, table_id=file_id)
                html_filename = os.path.join(output_dir, os.path.basename(file_path).replace('.csv', '.html'))
                with open(html_filename, 'w') as f:
                    f.write(f"""
                    <!DOCTYPE html>
                    <html>
                    <head>
                        <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
                        <title>{os.path.basename(file_path)}</title>
                    </head>
                    <body>
                        <div class="container">
                            <h1>{os.path.basename(file_path)}</h1>
                            {html_table}
                        </div>
                    </body>
                    </html>
                    """)
        EOF

    - name: Convert CSV to Excel
      run: |
        python - <<EOF
        import pandas as pd
        import os

        # Create the output directory if it doesn't exist
        output_dir = "doc/excel_output"
        os.makedirs(output_dir, exist_ok=True)

        # Function to write multiple CSVs to a single Excel file
        def write_to_excel(output_file, sheets):
            with pd.ExcelWriter(output_file, engine='openpyxl') as writer:
                for sheet_name, file_path in sheets.items():
                    if os.path.exists(file_path):
                        df = pd.read_csv(file_path)
                        df.to_excel(writer, index=False, sheet_name=sheet_name)

        # Background data
        background_sheets = {
            "BackgroundDB_SourceDatasets_ecoinvent": "doc/BackgroundDB_SourceDatasets_ecoinvent.csv",
            "BackgroundDB_SourceDatasets_GaBi": "doc/BackgroundDB_SourceDatasets_GaBi.csv"
        }
        write_to_excel("doc/excel_output/BackgroundData.xlsx", background_sheets)

        # Indicator data
        indicator_sheets = {
            "Common_references": "doc/Common_references.csv",
            "Country-specific_indicators": "doc/Country-specific_indicators.csv",
            "EN15804+A1_indicators": "doc/EN15804+A1_indicators.csv",
            "EN15804+A2_indicators": "doc/EN15804+A2_indicators.csv",
            "Flow_properties_and_unit_groups": "doc/Flow_properties_and_unit_groups.csv"
        }
        write_to_excel("doc/excel_output/Indicators.xlsx", indicator_sheets)
        EOF

    - name: Check output
      run: |
        echo "Listing files in doc/html_output directory"
        ls -l doc/html_output || echo "doc/html_output directory not found or is empty"
        echo "Listing files in doc/excel_output directory"
        ls -l doc/excel_output || echo "doc/excel_output directory not found or is empty"

    - name: Commit and push HTML and Excel files
      if: success()
      run: |
        git config --global user.name 'github-actions[bot]'
        git config --global user.email 'github-actions[bot]@users.noreply.github.com'
        git add doc/html_output/*.html doc/excel_output/*.xlsx || exit 0
        git commit -m 'Automated conversion of CSV to HTML and Excel'
        git push origin HEAD:gitActions
